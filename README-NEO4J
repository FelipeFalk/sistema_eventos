1. Baixar e instalar o Neo4J desktop: https://neo4j.com/download-thanks-desktop/?edition=desktop&flavour=winstall64&release=1.5.9&offline=true
2. Criar um Database
3. Executar o script de criação e carregamento, os arquivos csvs devem ser salvos previamente no diretório .\Neo4J\Neo4JData\relate-data\dbmss\dbms(Aqui vai depender do seu projeto)\import
4. Instalar o Python
5. Alterar a URI, Usuário e Senha do mainNeo4J.py
6. Instalar as dependencias do Python no projeto (pip install -r requirements.txt, pip install neo4j, pip install tabulate, pip install prettytable) 
7. Rodar o programa (python mainNeo4J.py)
8. Código Auxiliar (pip freeze > requirements.txt) | (pip --version)
